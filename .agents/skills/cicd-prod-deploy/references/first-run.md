# Pierwsze wykonanie — 2026-09-18

Zlecenie: prod deploy bieżącej animacji menu, zapis wszystkich kroków i utworzenie skilla o małym koszcie tokenów.

1. Odczytano `skill-creator/SKILL.md`, lessons, zatwierdzony deploy-plan, instrukcję deploymentu, verification, workflow, package.json, kontrolę limitów Pages i reguły uprawnień. Sprawdzono status, branch, SHA, origin oraz dostępne narzędzia. Nie odczytywano sekretów do rozmowy.
2. Potwierdzono czyste `main`: `677445f446a8e081c22463741d8890f5cc1ae310`, zgodne z `origin/main`. Użytkownik wcześniej commitował i pushował animację. Nie wykonano kolejnego commita/pusha.
3. `node --use-system-ca .cache/release-status.mjs` potwierdził udaną instalację, walidację i publikację w istniejącym [GitHub Actions run](https://github.com/pdrozdowski/MilkyWayTrader/actions/runs/35315948385), job `105507599633`. Nie powielono deploymentu. CI wykonało `npm ci`, typecheck, audyt HIGH/CRITICAL, `build-nolog`, kontrolę Pages i Wrangler Pages deploy.
4. Odczytano lokalne wcześniejsze narzędzia QA i przygotowano samodzielnego skilla w ignorowanym `.cache/cicd-prod-deploy/`: krótki SKILL.md, metadane UI, główny skrypt oraz QA Chrome/Edge przez CDP. Skrypty korzystają tylko z wbudowanych modułów Node; nie instalowano nowych zależności.
5. Sprawdzono oba skrypty przez `node --check` oraz ścieżkę `--help`. Walidator skilla w Python 3.14 zgłosił brak PyYAML; istniejący Python 3.10 z PyYAML 6.0.1 poprawnie wykonał `quick_validate.py`.
6. Pierwsze wykonanie skryptu odczytało canonical production z Cloudflare i sprawdziło grafiki. Zatrzymało się na fałszywej rozbieżności `style.css`: Windows checkout miał 2047 bajtów, Linux CI 2046. Diagnostyka publicznego HTTP i Git potwierdziła końce linii. Porównanie poprawiono na SHA-256 bajtów commita, przez `git cat-file blob`, zamiast lokalnego checkoutu. Nie zmieniono aplikacji.
7. Druga próba wykryła domyślny limit bufora Node przy odczycie dużego tła. Zwiększono bufor do 32 MiB. Te próby były wyłącznie odczytami istniejącego udanego wydania; żadna nie ponawiała publikacji.
8. Trzecia próba: PASS. Cloudflare canonical deployment `00ae1330-d80b-4178-8e8a-dc83d6cc91e0`, UTC `2026-09-18T06:42:03.917532Z`, produkcja/main, prawidłowy SHA, dirty=false, deploy success. URL: https://milky-way-trader.pages.dev/; immutable URL: https://00ae1330.milky-way-trader.pages.dev. Poprzedni udany prod / kandydat rollbacku: `b5eaa98e-e00f-46c7-b02b-3cc60cd8d631`; rollbacku nie wykonano.
9. Wszystkie 6 publicznych plików miało SHA-256 zgodne z commitem. Chrome 152.0.7977.84 i Edge 153.0.4234.32: PASS; zarejestrowano przeloty w menu, MainMenu → Game → GameOver → MainMenu i reload. Brak błędów JS/konsoli, nieudanych requestów i HTTP >=400. TLS pozostał włączony; Node używał systemowego CA.
10. Ponownie potwierdzono niezmienność HEAD i canonical deployment. Zapisano szczegółowy log `.cache/prod-deploy/2026-09-18T06-47-05-955Z-677445f/run.json`, wyniki i 12 zrzutów w `screenshots/` obok logu; raport: `context/deployment/releases/2026-09-18T06-47-05-955Z-677445f.md`. Wizualnie sprawdzono Chrome upward/downward/game oraz Edge returned-menu: grafiki, warstwy i sceny poprawne.
11. Dopracowano preflight, aby własne raporty Markdown nie blokowały kolejnego uruchomienia. Pozostałe zmiany użytkownika nadal blokują automatyczne wydanie. Uzupełniono raport o wizualną weryfikację i zapisano tę historię poza główną instrukcją skilla.
12. Zainstalowano skilla w `C:/Users/pdroz/.codex/skills/cicd-prod-deploy/`, zwalidowano zainstalowany SKILL.md oraz składnię i pomoc skryptów. Nie zmieniano workflow, kont, planów, sekretów ani produkcyjnych baz. Nie używano curl/wget, git push, reset, force push lub usuwania projektów.

Powtarzalna procedura: jedno wywołanie skryptu, krótki wynik, obejrzenie zrzutu menu. Historia i pełne logi ładowane tylko przy diagnostyce. Ścieżka dispatch (gdy nie istnieje push run) wymaga `--deploy-authorized`; podczas tej sesji nie była wywoływana na żywo, aby nie tworzyć zbędnego wydania.
