# Public browser configuration for the future Supabase integration.
# Copy to .env.local; the starter currently does not use these values.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Never put Cloudflare tokens, Google client secrets or Supabase secret keys here.
